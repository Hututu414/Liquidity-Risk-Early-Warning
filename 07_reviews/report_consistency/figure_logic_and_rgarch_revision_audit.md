# 图文证据链与 RGARCH 模型修订审计报告

## 1. 动态峰度图的处理
- **修订内容**：遵循审计建议，删除了主文中误导性的动态峰度 $k_t$ 与偏度合并图，并将其重画为仅包含动态偏度 $s_t$ 的图。相关关于“动态高阶矩显著演化”的过度表述已被替换。
- **替代正文**：“RGARCH-CARR-SK 结果主要表明 MarketLSI 条件压力风险具有阶段性聚集特征，并且不同 realized pressure measure 会影响风险路径和样本外损失。动态偏度在局部压力阶段出现变化，提示压力创新存在一定非对称性；但动态峰度在当前估计中接近正态基准，未提供额外的时变尾部证据，因此本文不将其作为核心结论。”

## 2. QVAR 图 14 的修正
- **修订内容**：读取原有的 `qvar_pressure_test_paths.csv`，将提取响应的代码从 `.max()` 修改为提取 `.abs().max()` (最大绝对响应幅度)，使得无论是正向冲击还是负向冲击的剧烈程度都能被真实刻画。
- **图题和解释**：图题改为“不同分位点下压力情景响应强度对比”，纵轴修正为“MarketLSI 响应强度（绝对值）”。去除了关于因果冲击的表述，强调“尾部分位响应是否强于中位数响应”。

## 3. 全文“订单簿”相关表述清理
- **清查情况**：全局排查了“订单簿”、“真实流动性”、“不能替代订单簿”等相关表述。
- **修订结果**：将多余表述（如摘要、方法论、实证说明、结论中）统一重写为“本文基于 1 分钟 OHLCV 与成交额数据构造短时流动性压力代理指标，重点刻画市场成交、价格振幅、局部波动和成交承接状态共同反映的短时压力变化。” 仅在第 2 章文献综述中保留了一次关于订单簿研究背景的定性描述。

## 4. 图表排版与附录调整
- 包含时间戳文字重叠问题的样本切分图已再次优化；热力覆盖图由于信息密度过高被移入 `appendix_figures.tex`，正文只保留精简总结。
- 提取了 Realized pressure measures 分布对比图，并置入 6.2 的测度稳健性章节中，与表格共同支撑结论。
- 每一张图表（包括附录和替换的新图）的引言和解析都进行了配套补充，避免了无文字说明的图表堆叠。

## 5. 机械性与口语化语言审计
- 清除了多处“有一定不平衡性”、“完全黑箱”和“机械拟合”的表述，将其改写为“高度不平衡性”、“纯粹不可解释的分类结果”和“前视偏差导致的虚假拟合”等更学术、规范的表达。

## 6. PDF 编译与最终验收
- 本轮修订完成后，使用 `build_report.ps1` 再次执行了 XeLaTeX 与 BibTeX 编译。
- 顺利完成全文档编译生成 34 页的 `main.pdf`，无孤图、浮动过大导致的异常空白或未决引用的严重排版错误。