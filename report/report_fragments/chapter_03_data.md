# 第 3 章 数据来源与变量处理

## 3.1 数据来源
本文研究样本为 A 股市场具有代表性的 84 只证券，涵盖了不同行业与市值的头部公司。样本区间自 2023 年 5 月 15 日起至 2026 年 5 月 13 日止。
原始数据采用 1 分钟高频 OHLCV（开盘价、最高价、最低价、收盘价、成交量）及成交额数据。数据来源于高频行情数据库，并经过初步清洗，统一了字段命名与证券代码格式。

## 3.2 样本清洗与就绪处理
为了保证高频研究的有效性，我们对原始面板进行了以下处理：
1. **交易时段过滤**：仅保留连续竞价时段数据（09:30-11:30, 13:00-14:57），剔除集合竞价及非交易时段。
2. **分钟完整性剔除**：针对每个“证券-日期”对，要求有效分钟数不低于 236 分钟（理论值为 240 分钟）。若因停牌或数据缺失导致有效分钟数不足，则剔除该日样本。
3. **特征槽位（Slot）构造**：将每日 240 个交易分钟映射到固定槽位，用于捕捉流动性的日内模式。

## 3.3 核心组件构造
基于分钟级数据，我们构造了反映流动性四个维度的核心组件（k 取值 5, 10, 20）：
1. **紧致性 (Tightness)**：采用 `Range(k)`，即 k 分钟内最高价与最低价之比的对数均值。
2. **深度 (Depth)**：采用 `ILLIQ(k)`，即收益率绝对值与成交额之比的均值。
3. **波动性 (Volatility)**：采用 `RV(k)`，即分钟收益率平方和。
4. **成交活跃度 (Activity)**：采用 `RelAmt(k)`，即成交额的对数均值。

## 3.4 样本划分
为防止前瞻性偏差（Look-ahead Bias），本文严格按时间顺序划分样本：
- **训练期**：2023-05-15 至 2025-02-28（约 60%）
- **验证期**：2025-03-03 至 2025-09-26（约 20%）
- **测试期**：2025-09-29 至 2026-05-13（约 20%）

所有标准化参数（Median, MAD）及压力标签阈值均仅基于训练期数据计算。
